{
  "//": "Editor + typecheck config for the web tests. Mirrors apps/api/tsconfig.spec.json: composite off, nothing emitted, jest types added. apps/web/tsconfig.json stays spec-free so the Vite build output is clean.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "types": ["vite/client", "jest", "node"],
    "composite": false,
    "incremental": false,
    "declaration": false,
    "declarationMap": false,
    "noEmit": true
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["dist", "dist-types", "node_modules"]
}
