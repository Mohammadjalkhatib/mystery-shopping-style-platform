{
  "name": "@msp/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared DTO types only. No runtime dependencies by contract (CLAUDE.md section 4).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  }
}
